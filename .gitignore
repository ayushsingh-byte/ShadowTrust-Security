# --- Python ---
__pycache__/
*.py[cod]
*.pyd
*.so
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Packaging / build
build/
dist/
wheelhouse/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pytype/
.pyre/

# Coverage
.coverage
coverage.xml
htmlcov/

# --- Environment / secrets ---
# NOTE: env files are NOT ignored (user requested to commit them).
*.pem
*.key
*.p12
*.pfx

# --- Logs / runtime artifacts ---
*.log
logs/

# Common runtime scratch
*.pid
*.pid.lock

# --- Databases / local state ---
# NOTE: database files are NOT ignored (user requested to commit them).

# Ignore SQLite sidecar files (generated at runtime)
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
*.db-journal
*.db-wal
*.db-shm

# Local scan outputs (runtime)
scans/*.json
backend/scans/

# Stray duplicate env files (previously removed)

# --- Frontend / Node ---
node_modules/

# TypeScript / Node build caches
*.tsbuildinfo
.eslintcache
.parcel-cache/
.turbo/
.next/

# --- OS / Editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Misc ---
*.swp
*.swo
*.tmp
